version: "3.8"
services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio:latest
    container_name: portfolio
    ports:
      - "2368:80"
    restart: unless-stopped
